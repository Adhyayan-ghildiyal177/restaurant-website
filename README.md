# DeltaX Food — Streamlit Food Ordering Website

Original food-ordering demo website with location, search, filters, restaurant cards, menu, cart and demo checkout.

## Run
```bash
pip install -r requirements.txt
streamlit run app.py
```
